"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findTokenByPosition = void 0;
function findTokenByPosition(tokens, line, column) {
    let left = 0;
    let right = tokens.length - 1;
    while (left <= right) {
        const m = Math.floor((left + right) / 2);
        const token = tokens[m];
        if (token.line < line || (token.line === line && token.column < column)) {
            left = m + 1;
        }
        else if (token.line > line || (token.line === line && token.column > column)) {
            right = m - 1;
        }
        else {
            return token;
        }
    }
    return undefined;
}
exports.findTokenByPosition = findTokenByPosition;
//# sourceMappingURL=tokenStream.js.map